<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SocialGift" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Estilo del SearchView -->
        <item name="android:background">@drawable/search_view_outline</item>
        <!-- Color del borde del SearchView -->
        <item name="android:textColor">@color/black</item>
        <!-- Estilo del icono del SearchView -->
        <item name="searchIcon">@drawable/ic_search_24</item>
    </style>

    <style name="Theme.SocialGift" parent="Base.Theme.SocialGift" />
</resources>